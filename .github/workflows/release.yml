name: Build & Package

on:
  push:
    tags: ["v*"]
    branches: ["pipeline_start"]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    container:
      image: joda001/evanalyzer-toolchain:0.1.5
      credentials:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry & build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-x86_64

      - name: Set version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev"
          fi
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Download native dependencies
        run: ./libs/download.sh linux-cpu

      - name: Unzip libtorch
        run: unzip -q "libs/libtorch-"*.zip -d libs

      - name: Build
        run: cargo build-linux

      - name: Package
        run: |
          mkdir -p dist/java dist/templates
          cp target/x86_64-unknown-linux-gnu/release/evanalyzer   dist/
          # libtorch is dynamically linked; ship every shared object next to the
          # binary so the $ORIGIN rpath (see .cargo/config.toml) finds them. The
          # glob must be *.so* (not *.so) to include versioned deps like
          # libgomp-<hash>.so.1 that libtorch_cpu.so requires.
          cp libs/libtorch/lib/*.so*                               dist/ 2>/dev/null || true
          unzip -q libs/jre_linux.zip -d dist/java/jre_linux
          cp crates/core/java/src/bioformats.jar                   dist/java/
          cp crates/core/java/BioFormatsWrapper.class              dist/java/
          cp templates/*.evapipe templates/*.evapt                 dist/templates/
          tar -czf evanalyzer-linux-x86_64.tar.gz -C dist .

      - uses: actions/upload-artifact@v4
        with:
          name: evanalyzer-linux-x86_64
          path: evanalyzer-linux-x86_64.tar.gz

  build-windows:
    runs-on: ubuntu-latest
    container:
      image: joda001/evanalyzer-toolchain:0.1.5
      credentials:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry & build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: windows-x86_64

      - name: Set version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev"
          fi
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Download native dependencies
        run: ./libs/download.sh win-cpu

      - name: Unzip DuckDB Windows library
        run: unzip -q libs/libduckdb-windows-amd64.zip -d libs/libduckdb-windows-amd64

      - name: Unzip libtorch
        run: unzip -q "libs/libtorch-"*.zip -d libs

      - name: Build
        run: cargo build-win

      - name: Package
        run: |
          mkdir -p dist/java dist/templates
          cp target/x86_64-pc-windows-msvc/release/evanalyzer.exe dist/
          cp libs/libduckdb-windows-amd64/duckdb.dll           dist/
          cp libs/libtorch/lib/*.dll                           dist/
          unzip -q libs/jre_win.zip -d dist/java/jre_win
          cp crates/core/java/src/bioformats.jar                   dist/java/
          cp crates/core/java/BioFormatsWrapper.class              dist/java/
          cp templates/*.evapipe templates/*.evapt                 dist/templates/
          (cd dist && zip -r ../evanalyzer-windows-x86_64.zip .)

      - uses: actions/upload-artifact@v4
        with:
          name: evanalyzer-windows-x86_64
          path: evanalyzer-windows-x86_64.zip

  build-linux-cuda:
    runs-on: ubuntu-latest
    container:
      image: joda001/evanalyzer-toolchain:0.1.5
      credentials:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry & build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-x86_64-cuda

      - name: Set version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev"
          fi
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Download native dependencies
        run: ./libs/download.sh linux-cuda

      - name: Unzip libtorch
        run: unzip -q "libs/libtorch-"*.zip -d libs

      - name: Build
        run: cargo build-linux

      - name: Package
        run: |
          mkdir -p dist/java dist/templates
          cp target/x86_64-unknown-linux-gnu/release/evanalyzer   dist/
          # CUDA libtorch ships its runtime as versioned objects (libcudart.so.12,
          # libcublas.so.12, libcudnn*.so.9, libnccl.so.2, libgomp-<hash>.so.1, …),
          # so the glob must be *.so* to catch the .so.N files - a plain *.so would
          # ship none of the CUDA libs and the binary would fail to start.
          cp libs/libtorch/lib/*.so*                               dist/ 2>/dev/null || true
          unzip -q libs/jre_linux.zip -d dist/java/jre_linux
          cp crates/core/java/src/bioformats.jar                   dist/java/
          cp crates/core/java/BioFormatsWrapper.class              dist/java/
          cp templates/*.evapipe templates/*.evapt                 dist/templates/
          tar -czf evanalyzer-linux-x86_64-cuda.tar.gz -C dist .

      - uses: actions/upload-artifact@v4
        with:
          name: evanalyzer-linux-x86_64-cuda
          path: evanalyzer-linux-x86_64-cuda.tar.gz

  build-windows-cuda:
    runs-on: ubuntu-latest
    container:
      image: joda001/evanalyzer-toolchain:0.1.5
      credentials:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry & build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: windows-x86_64-cuda

      - name: Set version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev"
          fi
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Download native dependencies
        run: ./libs/download.sh win-cuda

      - name: Unzip DuckDB Windows library
        run: unzip -q libs/libduckdb-windows-amd64.zip -d libs/libduckdb-windows-amd64

      - name: Unzip libtorch
        run: unzip -q "libs/libtorch-"*.zip -d libs

      - name: Build
        run: cargo build-win

      - name: Package
        run: |
          mkdir -p dist/java dist/templates
          cp target/x86_64-pc-windows-msvc/release/evanalyzer.exe dist/
          cp libs/libduckdb-windows-amd64/duckdb.dll           dist/
          cp libs/libtorch/lib/*.dll                           dist/
          unzip -q libs/jre_win.zip -d dist/java/jre_win
          cp crates/core/java/src/bioformats.jar                   dist/java/
          cp crates/core/java/BioFormatsWrapper.class              dist/java/
          cp templates/*.evapipe templates/*.evapt                 dist/templates/
          (cd dist && zip -r ../evanalyzer-windows-x86_64-cuda.zip .)

      - uses: actions/upload-artifact@v4
        with:
          name: evanalyzer-windows-x86_64-cuda
          path: evanalyzer-windows-x86_64-cuda.zip

  build-macos:
    runs-on: macos-14   # Apple Silicon (arm64)
    steps:
      - uses: actions/checkout@v4

      - name: Cache Cargo registry & build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: macos-arm64

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev"
          fi
          sed -i '' "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Download native dependencies
        run: ./libs/download.sh mac-cpu

      - name: Unzip libtorch
        run: unzip -q "libs/libtorch-"*.zip -d libs

      - name: Unzip DuckDB macOS library
        run: unzip -q libs/libduckdb-osx-universal.zip -d libs/libduckdb-osx-universal

      - name: Build
        env:
          # DuckDB is not bundled on macOS; point libduckdb-sys at the prebuilt lib.
          DUCKDB_LIB_DIR: ${{ github.workspace }}/libs/libduckdb-osx-universal
          DUCKDB_INCLUDE_DIR: ${{ github.workspace }}/libs/libduckdb-osx-universal
        run: cargo build-mac

      - name: Package
        run: |
          mkdir -p dist/java dist/templates
          cp target/aarch64-apple-darwin/release/evanalyzer       dist/
          # Ship libtorch + DuckDB dylibs next to the binary; the @loader_path
          # rpath (see .cargo/config.toml) resolves them at runtime.
          cp libs/libtorch/lib/*.dylib                             dist/ 2>/dev/null || true
          cp libs/libduckdb-osx-universal/*.dylib                  dist/ 2>/dev/null || true
          unzip -q libs/jre_macos_arm.zip -d dist/java/jre_macos_arm
          cp crates/core/java/src/bioformats.jar                   dist/java/
          cp crates/core/java/BioFormatsWrapper.class              dist/java/
          cp templates/*.evapipe templates/*.evapt                 dist/templates/
          tar -czf evanalyzer-macos-arm64.tar.gz -C dist .

      - uses: actions/upload-artifact@v4
        with:
          name: evanalyzer-macos-arm64
          path: evanalyzer-macos-arm64.tar.gz

  release:
    needs: [build-linux, build-windows, build-linux-cuda, build-windows-cuda, build-macos]
    runs-on: ubuntu-latest
    # Only publish a GitHub release when triggered by a version tag
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/evanalyzer-linux-x86_64/evanalyzer-linux-x86_64.tar.gz
            artifacts/evanalyzer-windows-x86_64/evanalyzer-windows-x86_64.zip
            artifacts/evanalyzer-linux-x86_64-cuda/evanalyzer-linux-x86_64-cuda.tar.gz
            artifacts/evanalyzer-windows-x86_64-cuda/evanalyzer-windows-x86_64-cuda.zip
            artifacts/evanalyzer-macos-arm64/evanalyzer-macos-arm64.tar.gz
